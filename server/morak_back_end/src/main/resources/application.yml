spring:
  jpa:
    hibernate:
      ddl-auto: create # create update create-update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database: mysql
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
  datasource:
    url: jdbc:mysql://localhost:3306/back?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: asdaszxcwqweqweqwe
            client-secret: asddaasdxczxczxcxc
            scope:
              - read:user
              - user:email
          google:
            client-id: dassasddasxcxczxcasd
            client-secret: dasdasdzxczxczxczxc
            scope:
              - email
              - profile
  mail:
    host: smtp.gmail.com
    username: aaaa@naver.com
    password: asdddss!!!DD
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
            protocols: TLSv1.2

logging:
  level:
    org.springframework.web.client: debug
    org.hibernate.sql: debug
    org.hibernate.type: trace
    org.springframework.security: debug

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true

jwt:
  secretKey: jwt-tokens-that-should-be-changed-production
  refreshKey: jwt-refresh-tokens-that-should-be-changed-production